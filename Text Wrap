
# Qs Link --> https://www.hackerrank.com/challenges/text-wrap/problem

def wrap(string, max_width):
    if len(string)> 0 and len(string)<1000:
        if max_width > 0 and max_width < len(string):
            result = "\n".join(textwrap.wrap(string,max_width))
    return result
